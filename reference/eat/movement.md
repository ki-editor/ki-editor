Perform the {movement} movement. Any text that is contained within the new selection, old selection, or between them, is replaced with the new selection.

Suppose the current selection mode is WORD and square brackets denote the current selection. Then:

```
{old}
```

becomes:

```
{new}
```

Also, in syntax selection mode, some checks are done to ensure the result is still syntactically valid. 