import {TutorialFallback} from '@site/src/components/TutorialFallback';
import {KeymapFallback} from '@site/src/components/KeymapFallback';

# Insert MoL

The actions of this MoL enters Insert mode.

## Keymap

<KeymapFallback filename="Insert"/>

## Tap action: Change

Deletes the current selection and enters Insert mode.

<TutorialFallback filename="change"/>

## Insert

Enters Insert mode without any text modifications.

<TutorialFallback filename="insert"/>

## Open

This is similar to how [Paste MoL](./paste-mol.mdx) works, where gaps will be automatically inserted.

Open works by analyzing the gap between the current selection and the
`<`/`>`/`<<`/`>>` selection, then insert the gap before/after the current selection.

<TutorialFallback filename="open"/>
